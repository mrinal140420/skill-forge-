package com.skillforge.dto;

/**
 * Auth DTOs - Split into separate files for proper Java structure
 * See: RegisterRequestDTO, LoginRequestDTO, AuthResponseDTO, JwtClaimsDTO, ErrorResponseDTO
 */
public class AuthDTO {
    // Placeholder class - actual DTOs are in separate files
}
